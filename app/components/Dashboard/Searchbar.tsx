'use client';

import React from 'react';
import { Input } from '../Input';

const Search = () => {
  return (
    <div className='relative'>
      <Input />
    </div>
  );
};

export default Search;
